<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= course.name %></title>

  <style>
    body {
      font-family: 'Arial', sans-serif;
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    header {
      background-color: #333;
      color: #fff;
      padding: 10px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    h1 {
      margin: 0;
    }

    nav ul {
      list-style-type: none;
      padding: 0;
      margin: 0;
      display: flex;
    }

    nav ul li {
      margin-right: 20px;
    }

    nav ul li:last-child {
      margin-right: 0;
    }

    nav a {
      text-decoration: none;
      color: #fff;
      font-weight: bold;
    }

    #editButton {
      background-color: orange;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    #delButton {
      background-color: orange;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    #joinButton {
      background-color: green;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }


    #leaveButton {
      background-color: rgb(128, 0, 0);
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }
  </style>
</head>

<body>

  <header>
    <h1><%= course.name %></h1>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/dashboard">Dashboard</a></li>
      </ul>
    </nav>
    <% if(isCreator) { %>

    <button id="editButton">Edit Document</button>
    <button id="delButton">Delete Document</button>

    <script>
      document.getElementById('delButton').addEventListener("click", async () => {
        await fetch(window.location.pathname, {
          method: 'DELETE'
        }).then(data => {
          console.log(data)
        }).catch(error => {
          console.error('Error:', error);
        });
      })
    </script>

    <% } else if (!isMember) { %>

    <button id="joinButton">Join Course</button>
    <script>
      document.getElementById('joinButton').addEventListener("click", async () => {
        await fetch(window.location.pathname, {
          method: 'POST'
        }).then(data => {
          console.log(data)
        }).catch(error => {
          console.error('Error:', error);
        });
      })
    </script>


    <% } else { %>

    <button id="leaveButton">Leave Course</button>


    <% } %>
  </header>

  <div style="margin-left: 20%; margin-top: 3%;">
    <%- course.code %>

  </div>




</body>

</html>